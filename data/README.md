# Data Directory

This directory stores time series data and datasets.

## Structure

- `raw/` - Raw metric data from sources
- `processed/` - Preprocessed and feature-engineered data
- `labeled/` - Labeled samples for supervised learning
- `synthetic/` - Generated synthetic data for testing

## Note

Data files are excluded from version control via `.gitignore`.
Only the directory structure is tracked.
